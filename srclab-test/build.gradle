plugins {
    id "org.jetbrains.kotlin.jvm"
}

description = "Test of SrcLab Common"

apply from: gradles.compile

apply from: gradles.repositories

java {
    registerFeature('optionalSupport') {
        usingSourceSet(sourceSets.main)
    }
}

dependencies {
    implementation(platform(project(":srclab-common-parent")))

    implementation(project(":srclab-annotations"))

    implementation("org.jetbrains.kotlin:kotlin-stdlib-jdk8")
    implementation("org.apache.commons:commons-lang3")
    implementation("com.google.guava:guava")
    implementation("org.testng:testng")

    testCompile("org.testng:testng")
}

test {
    useTestNG()
    include("**/*Test.class")
}

apply from: gradles.deployJar