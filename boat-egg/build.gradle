plugins {
  id 'build.jar'
}

description = "Happy Eggy!"

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  implementation project(":boat-core")
  implementation project(":boat-data")

  testImplementation project(":boat-test")
}