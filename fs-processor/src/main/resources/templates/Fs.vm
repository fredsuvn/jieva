package xyz.srclab.common.base;

#foreach($im in $ims)
import $im;
#end

/**
 * Common utilities, collected from
 * {@link FsObject}, {@link FsCheck}, {@link FsString} and etc..
 *
 * @author fredsuvn
 */
public class Fs {
#foreach($m in $methods)

    /**
    *$m.comment
    */
    public static #if($m.typeParams)<#foreach($t in $m.typeParams)$t#if($velocityCount<$foreach.count), #end#end> #end$m.returnType ${m.methodName}({params})} {
    {needReturn}{fsName}.{methodName}({args});
    }
#end
}