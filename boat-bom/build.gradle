plugins {
  id 'build.kotlin-publish-platform'
}

description = "Boat BOM"

javaPlatform {
  allowDependencies()
}

dependencies {
  constraints {
    file("${rootDir}").eachDirMatch(~/boat-.*/) { dir ->
      api(project(":${dir.name}"))
    }
  }
}