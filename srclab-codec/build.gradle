plugins {
    id "org.jetbrains.kotlin.jvm"
}

description = "SrcLab Common Codec"

apply from: gradles.compile

apply from: gradles.repositories

java {
    registerFeature('optionalSupport') {
        usingSourceSet(sourceSets.main)
    }
}

dependencies {
    implementation(platform(project(":srclab-common-bom")))

    implementation(project(":srclab-common"))

    testCompile(project(":srclab-test"))
}

test {
    useTestNG()
    include("**/*Test.class")
}

apply from: gradles.deployJar