plugins {
  id 'fs-build'
  id 'build.compile'
}

project.afterEvaluate {

  fs.info("${project.name}: import build.sample")

  test {
    useTestNG()
    include("**/*Sample.class", "**/*SampleKt.class")
  }
}