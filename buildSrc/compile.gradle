compileKotlin {
    kotlinOptions {
        jvmTarget = "${javaVersion}"
        freeCompilerArgs = ['-Xjvm-default=compatibility']
    }
}
compileTestKotlin {
    kotlinOptions {
        jvmTarget = "${javaVersion}"
        freeCompilerArgs = ['-Xjvm-default=compatibility']
    }
}