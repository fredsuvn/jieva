plugins {
    id 'java-platform'
}

description = "Easy for Parent"

apply from: gradles.repositories

javaPlatform {
    allowDependencies()
}

dependencies {
    api platform(project(":easy-bom"))

    constraints {
    }
}

apply from: gradles.deployPlatform