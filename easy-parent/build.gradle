plugins {
    id 'java-platform'
}

description = "easy-common: Parent"

apply from: gradles.repositories

javaPlatform {
    allowDependencies()
}

dependencies {
    api platform(project(":easy-bom"))

    constraints {
    }
}

apply from: gradles.deployPlatform