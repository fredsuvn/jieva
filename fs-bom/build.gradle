plugins {
  id 'build.platform'
}

description = "Fs BOM"

javaPlatform {
  allowDependencies()
}

dependencies {
  constraints {
    //api project(":boat")
    file("${rootDir}").eachDirMatch(~/fs-(?!plugins).*/) { dir ->
      api project(":${dir.name}")
    }
  }
}