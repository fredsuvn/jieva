pluginManagement {
  repositories {
    gradlePluginPortal()
  }
  plugins {
    id "org.jetbrains.kotlin.jvm" version "${kotlinVersion}"
  }
}

rootProject.name = 'boat-build'

//Including boat projects
file("${rootDir}/").eachDirMatch(~/boat-.*/) { file ->
  include ":${file.name}"
}