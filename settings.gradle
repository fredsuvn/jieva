pluginManagement {
  repositories {
    maven {
      url "https://maven.aliyun.com/repository/gradle-plugin"
    }
    mavenCentral()
    gradlePluginPortal()
  }
}

rootProject.name = 'fs-root'
file("${rootDir}/").eachDirMatch(~/fs-(?!(plugins|draft)).*/) { dir ->
  include ":${dir.name}"
}